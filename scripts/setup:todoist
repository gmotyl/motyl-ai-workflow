#!/bin/bash
echo "🔧 Setting up Todoist integration..."
echo ""
echo "Manual setup required. See: docs/integration-todoist.md"
echo ""
read -p "Enter your Todoist API token: " -s TODOIST_TOKEN
echo
if [ -z "$TODOIST_TOKEN" ]; then
    echo "❌ API token required"
    exit 1
fi
echo "export TODOIST_API_TOKEN='$TODOIST_TOKEN'" >> ~/.bashrc
echo "export TODOIST_API_TOKEN='$TODOIST_TOKEN'" >> ~/.zshrc
echo "✅ Todoist setup complete"
