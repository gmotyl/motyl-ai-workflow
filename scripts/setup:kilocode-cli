#!/bin/bash
echo "🔧 Setting up Kilocode CLI..."
echo ""
echo "Manual setup required. See: agents/kilocode-cli.md"
echo ""
read -p "Have you installed Kilocode CLI? (y/n) " -n 1 -r
echo
if [[ ! $REPLY =~ ^[Yy]$ ]]; then
    echo "Install Kilocode from: https://kilo.ai/"
    exit 1
fi
echo "✅ Kilocode CLI setup complete"
